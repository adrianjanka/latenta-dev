{
  "name": "latenta-dev",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "directus:setup": "tsx scripts/setup-directus-schema.ts",
    "data:fetch": "tsx scripts/fetch-film-api.ts",
    "data:transform": "tsx scripts/transform-film-api.ts",
    "data:enrich": "tsx scripts/enrich-filmstocks.ts",
    "data:seed": "tsx scripts/seed-directus.ts",
    "data:pipeline": "npm run data:fetch && npm run data:transform && npm run data:enrich",
    "test:recommendation": "tsx scripts/test-recommendation.ts"
  },
  "dependencies": {
    "@nuxtjs/tailwindcss": "^6.14.0",
    "nuxt": "^3.21.8",
    "vue": "^3.5.38",
    "vue-router": "^5.1.0"
  },
  "devDependencies": {
    "dotenv": "^17.4.2",
    "tsx": "^4.22.5"
  }
}
